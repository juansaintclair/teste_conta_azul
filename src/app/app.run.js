(function() {
    'use strict';

    angular
        .module('ca.weathernow')
        .run(AppRun);

    /* @ngInject */
    function AppRun($rootScope, $location, $transitions) {
        //Aguardando
    }
})();
